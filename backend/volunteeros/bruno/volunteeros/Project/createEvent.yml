info:
  name: createEvent
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/projects/:projectId/events"
  params:
    - name: projectId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "title": "Gather garbage in the city garden",
        "description": "A volunteer event to clean and improve public spaces in the local community.",
        "date": "2026-09-22",
        "startTime": "07:00:00",
        "endTime": "15:00:00",
        "location": "Berlin, Germany",
        "capacity": 20
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
