info:
  name: applyForProject
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/projects/:projectId/participants"
  params:
    - name: projectId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
