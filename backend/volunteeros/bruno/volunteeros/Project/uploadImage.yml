info:
  name: uploadImage
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/projects/:projectId/image"
  params:
    - name: projectId
      value: "2"
      type: path
  body:
    type: multipart-form
    data:
      - name: image
        type: file
        value:
          - /home/zenmaster/Downloads/moritz-lange-jIpTrVUVy90-unsplash.jpg
        contentType: image/jpeg
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
