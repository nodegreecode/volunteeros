info:
  name: activateProject
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/projects/:projectId/activate"
  params:
    - name: projectId
      value: "15"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
