info:
  name: getOrganization
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/organizations/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
