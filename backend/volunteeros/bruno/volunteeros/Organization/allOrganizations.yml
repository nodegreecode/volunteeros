info:
  name: allOrganizations
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/organizations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
