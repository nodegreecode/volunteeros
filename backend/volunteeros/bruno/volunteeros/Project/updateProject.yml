info:
  name: updateProject
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{baseUrl}}/api/projects/:projectId"
  params:
    - name: projectId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "location" : "Hamburg, Germany"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
