info:
  name: activateProject
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/projects/:projectId/active"
  params:
    - name: projectId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
