info:
  name: getEventRegistration
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/project-events/:eventId/registration"
  params:
    - name: eventId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
