info:
  name: updateProfile
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/api/users/profile"
  body:
    type: json
    data: |-
      {
        "city" : "Berlin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
