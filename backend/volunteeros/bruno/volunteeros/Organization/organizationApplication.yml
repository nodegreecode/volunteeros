info:
  name: organizationApplication
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/applications/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
