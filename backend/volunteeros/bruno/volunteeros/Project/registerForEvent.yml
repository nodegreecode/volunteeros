info:
  name: registerForEvent
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/project-events/:eventId/registrations"
  params:
    - name: eventId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
