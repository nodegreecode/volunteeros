info:
  name: updateparticipationsStatus
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/api/project-participations/:participationId/status"
  params:
    - name: participationId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "status" : "APPROVED"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
