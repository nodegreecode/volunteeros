info:
  name: addSkill
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/skills"
  body:
    type: json
    data: |-
      {
        "name" : "JavaScript",
        "proficiency" : "ADVANCED"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
