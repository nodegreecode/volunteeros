info:
  name: myProjects
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/projects/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
