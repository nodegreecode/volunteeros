info:
  name: createProject
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/projects"
  body:
    type: json
    data: |-
      {
        "title": "Bring out old stuf from school storage",
        "description": "A volunteer project focused on cleaning and improving public spaces in the local community.",
        "location": "Berlin, Germany",
        "startDate": "2026-09-16T09:00:00Z",
        "endDate": "2026-09-16T17:00:00Z",
        "requiredVolunteers": 20
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
