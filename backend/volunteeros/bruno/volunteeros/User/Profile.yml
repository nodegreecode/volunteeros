info:
  name: Profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/users/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
